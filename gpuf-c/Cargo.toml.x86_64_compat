[package]
name = "gpuf-c"
version = "0.1.0"
edition = "2021"

[lib]
name = "gpuf_c"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
jni = "0.21"
libc = "0.2"
log = "0.4"
env_logger = "0.10"

[target.'cfg(target_os = "android")'.dependencies]
android_logger = "0.13"

[features]
android = []
network = []  # x86_64 version disables network dependencies
default = ["android"]
