status: REVIEWED_REQUIRE_APP_LAYER_IMPLEMENTATION
generated_at_utc: 2026-06-10T02:31:17Z
branch: optimize/gpuf-c
commit: e1fc8b9eb58867f4e3749b827eca5b399ef45c7d

Review scope:
  gpuf-c/src/jni_remote_worker.rs
  gpuf-c/src/handle/android_sdk.rs
  docs/mobile-sdk/INTEGRATION_GUIDE_EN.md
  gpuf-c/docs/mobile/MOBILE_SDK_CHECKLIST.md

Findings:
  Native gpuf-c/JNI code does not implement long-lived credential persistence.
  No Android Keystore implementation was found in the native SDK layer.
  Documentation instructs production wrappers to avoid SharedPreferences/UserDefaults/plaintext storage for long-lived credentials.

Release conclusion:
  The native SDK does not store tokens itself.
  Production Android wrapper code must use Android Keystore for long-lived credentials and should pass only runtime-scoped values into JNI.
  This item is a review note, not proof that a downstream app wrapper has implemented Keystore storage.

Required closure evidence:
  To clear this blocker for formal mobile SDK release, replace the status with PASS_APP_LAYER_KEYSTORE_SIGNED_OFF and attach the production wrapper review details in this file or a linked release record.

Required fields:
  reviewer: <release-owner-or-security-reviewer>
  reviewed_at_utc: <timestamp>
  app_wrapper_repo_commit: <repo-or-artifact-id>@<commit-or-version>
  credential_storage: Android Keystore or hardware-backed equivalent for long-lived credentials
  runtime_secret_flow: only runtime-scoped values cross the JNI boundary
  forbidden_storage_checked: SharedPreferences, world-readable files, logs, manifests, assets, and arbitrary external storage
  log_redaction_checked: client IDs, tokens, prompts, model paths, endpoint hosts/IPs, CA paths, and pins are redacted or suppressed
  test_or_review_artifact: <CI job, test log, code review link, or signed release note>

Until those fields are attached by a release owner, this file must keep a blocking status.
