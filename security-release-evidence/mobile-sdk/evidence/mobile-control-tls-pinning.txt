status: PASS_UNIT_RECHECK_RUNTIME_LOG_FOLLOWUP
generated_at_utc: 2026-06-10T03:20:00Z
branch: optimize/gpuf-c
commit: 52e6697ee14fd5714a904280947f7861b84e7ee3

Purpose:
  Mobile control TLS and pinning evidence for Android/iOS C/JNI SDK surfaces.

API coverage:
  C header exposes gpuf_validate_mobile_tls_policy(ca_cert_path, control_tls_server_name, cert_sha256_pin).
  C header exposes start_remote_worker_with_tls(server_addr, control_port, proxy_port, worker_type, client_id, ca_cert_path, control_tls_server_name, cert_sha256_pin).
  Plain start_remote_worker remains available for backwards compatibility.
  Android JNI wrappers are additive and do not remove old plaintext calls.

Static review:
  gpuf-c/src/util/mobile_tls_policy.rs validates non-empty server name, CA bundle path, and SHA256 pin format.
  gpuf-c/src/util/mobile_control_stream.rs builds a TLS client config, loads CA roots, validates server name, and compares SHA256 pin when provided.
  gpuf-c/src/lib.rs maps TLS policy errors to stable C return codes.

Unit test command:
  cargo test -p gpuf-c mobile_tls_policy --lib

Unit test result on 2026-06-10 Linux re-check:
  running 7 tests
  passed: 7
  failed: 0

Passing tests:
  accepts_ca_bundle_and_sha256_pin
  accepts_colon_separated_pin_without_ca
  ffi_returns_stable_error_codes
  rejects_bad_pin
  rejects_missing_trust_material
  rejects_invalid_server_name
  rejects_missing_ca_file

Fixture status:
  gpuf-c/tests/fixtures/control-rotated-cert.pem is present and parses as a PEM certificate bundle.
  Valid CA-bundle and stable C FFI return-code tests now pass.

Previous iOS integration flow:
  run_ios_sim_tls_test.sh generates local TLS cert material and then starts the simulator sample with:
    GPUF_IOS_TEST_TLS=1
    GPUF_IOS_TEST_CA_CERT_SOURCE_PATH=<generated-ca-cert>
    GPUF_IOS_TEST_TLS_SERVER_NAME=<test-server-name>
  The sample calls start_remote_worker_with_tls and registers gpuf_register_remote_worker_callback.

Release conclusion:
  TLS APIs, negative validation coverage, valid CA-bundle parsing, SHA256 pin format handling, and stable C return codes are covered by passing unit tests.
  Attach a saved iOS TLS runtime log for human release review of the simulator/device flow.
